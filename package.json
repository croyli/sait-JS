{
  "scripts": {
    "serve": "parcel ./src/index.html -p 4200 --open",
    "build": "parcel build ./src/index.html --no-source-maps"
  },
  "devDependencies": {
    "assert": "^2.0.0",
    "parcel": "^2.7.0",
    "process": "^0.11.10"
  }
}
